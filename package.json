{
  "name": "clickdeck",
  "version": "1.3.5",
  "description": "Visually fine-tune the current HTML page and export it.",
  "private": true,
  "license": "MIT",
  "type": "module",
  "scripts": {
    "build": "vite build",
    "dev": "vite build --watch",
    "e2e": "playwright test",
    "package:release": "npm exec -- node scripts/package-release.mjs",
    "typecheck": "tsc --noEmit",
    "test": "vitest run src"
  },
  "devDependencies": {
    "@playwright/test": "^1.60.0",
    "@types/chrome": "^0.0.326",
    "@types/node": "^25.9.1",
    "jsdom": "^29.1.1",
    "typescript": "^5.9.3",
    "vite": "^7.2.4",
    "vitest": "^4.0.13"
  }
}
